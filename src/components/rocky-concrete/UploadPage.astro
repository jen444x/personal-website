---
import { Image } from "astro:assets";
import hisUploadPage from "../../assets/rocky-concrete/his-upload-page.png";
import sanityStudioDesk from "../../assets/rocky-concrete/sanity-studio-desk.png";
import Section from "./Section.astro";
import Prose from "./Prose.astro";
import Column from "./Column.astro";
import Wide from "./Wide.astro";
---

{/* Picking the CMS — folded into Build vs Buy. Commented out (not deleted) so the original copy isn't lost.
<!-- Picking the CMS -->
<section class="mb-24">
  <h2 class="text-3xl font-bold text-gray-900 mb-8">Picking the CMS</h2>

  <div class="space-y-5 text-gray-600 leading-relaxed text-lg max-w-3xl">
    <p>
      This was my first time working with a CMS, so I wanted something fast and
      easy to set up. Decap CMS (formerly Netlify CMS) was both, so I went with
      it. But it hit two walls, both from the same root: it was built for a
      developer, not for him.
    </p>
  </div>

  <!-- Decap · where it broke -->
  <div
    class="my-5 max-w-3xl rounded-lg border border-gray-200 bg-gray-50/70 px-4 py-3"
  >
    <p class="text-xs font-semibold text-gray-400 mb-2">Decap</p>
    <ul class="space-y-1.5 text-sm leading-relaxed text-gray-600">
      <li class="flex items-start gap-2">
        <svg
          class="mt-1 h-3.5 w-3.5 shrink-0 text-rose-500"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        <span>
          Git-based: his content lived in the repo, so every upload forced a
          full rebuild. <span class="font-semibold text-gray-800">Slow</span>,
          and it
          <span class="font-semibold text-gray-800">burned build minutes</span>.
        </span>
      </li>
      <li class="flex items-start gap-2">
        <svg
          class="mt-1 h-3.5 w-3.5 shrink-0 text-rose-500"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M6 18L18 6M6 6l12 12"></path>
        </svg>
        <span>Clunky on a phone, his only way to post.</span>
      </li>
    </ul>
  </div>

  <div class="space-y-5 text-gray-600 leading-relaxed text-lg max-w-3xl">
    <p>
      Trying to move fast ended up costing me more time: I built it out, then
      tore it back down. By then I'd already moved the project from GitHub Pages
      to Netlify and migrated the DNS. I cut it anyway.
    </p>
  </div>

  <!-- Sunk cost callout -->
  <p
    class="my-6 max-w-3xl text-xl font-semibold text-gray-900 leading-snug border-l-4 border-violet-400 pl-5"
  >
    Sunk cost is still a cost, but shipping the wrong tool is worse.
  </p>

  <div class="space-y-5 text-gray-600 leading-relaxed text-lg max-w-3xl">
    <p>
      So this time I researched my options against the problems I'd hit, and
      Sanity kept coming up as one of the best.
    </p>
  </div>

  <!-- Sanity · what I found -->
  <div
    class="my-5 max-w-3xl rounded-lg border border-violet-200 bg-violet-50/50 px-4 py-3"
  >
    <p class="text-xs font-semibold text-violet-700 mb-2">Sanity</p>
    <ul class="space-y-1.5 text-sm leading-relaxed text-gray-700">
      <li class="flex items-start gap-2">
        <svg
          class="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-600"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M5 13l4 4L19 7"></path>
        </svg>
        <span>
          API-based: content lives in a hosted dataset, so new photos show up
          with no rebuild.
        </span>
      </li>
      <li class="flex items-start gap-2">
        <svg
          class="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-600"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M5 13l4 4L19 7"></path>
        </svg>
        <span>One of the better studios on a phone.</span>
      </li>
      <li class="flex items-start gap-2">
        <svg
          class="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-600"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M5 13l4 4L19 7"></path>
        </svg>
        <span>
          Built-in crop and image hotspots, drag-to-reorder, conditional fields.
        </span>
      </li>
    </ul>
  </div>

  <div class="space-y-5 text-gray-600 leading-relaxed text-lg max-w-3xl">
    <p>
      I built out his schema and had him try it. It worked, but it was still too
      much for him to run on his own, which told me no off-the-shelf studio was
      going to fit him exactly.
    </p>
  </div>
</section>
*/}

<!-- His Upload Page -->
<Section title="His Upload Page">
  <!-- His page floats right; both text blocks flow beside it, then full width. -->
  <Column class="flow-root">
    <!-- Phone photo -->
    <figure class="mb-6 w-[230px] mx-auto sm:float-right sm:ml-10 sm:mb-3">
      <div class="rounded-[2rem] bg-gray-900 p-2 shadow-xl">
        <Image
          src={hisUploadPage}
          alt="Rocky Concrete's Spanish upload page on a phone, titled SUBIR FOTO: a photo picker, title field, category dropdown, and a notes field for Jennifer."
          width={480}
          class="block w-full h-auto rounded-[1.4rem]"
          loading="eager"
        />
      </div>
    </figure>

    <p class="mb-4">
      <span
        class="inline-block rounded-full bg-violet-100 text-violet-700 border border-violet-200 text-xs font-bold uppercase tracking-widest px-3.5 py-1.5"
      >
        What he sees · /upload
      </span>
    </p>
    <div class="space-y-4 text-gray-600 leading-relaxed text-lg">
      <p>
        I kept Sanity as the backend and built him his own door in front of it.
        His page only had to do one thing: let him add a photo of a finished
        job, the one thing he wanted from the site. One screen at <code
          class="font-mono text-[0.92em] text-violet-700 bg-violet-50 px-1.5 py-0.5 rounded"
          >/upload</code
        >, a few fields, submit.
      </p>
      <p>
        Behind the form, <code
          class="font-mono text-[0.92em] text-violet-700 bg-violet-50 px-1.5 py-0.5 rounded"
          >/api/upload</code
        > converts the iPhone's HEIC straight to JPEG on his phone (browsers won't
        render HEIC), so he sends a smaller file and my server never touches the
        original. Then it pushes the image to Sanity's CDN, creates the <code
          class="font-mono text-[0.92em] text-violet-700 bg-violet-50 px-1.5 py-0.5 rounded"
          >galleryItem</code
        > document, and copies his title into the alt text so the site stays accessible.
      </p>
    </div>

    <p class="mt-10 mb-4">
      <span
        class="inline-block rounded-full bg-gray-900 text-white text-xs font-bold uppercase tracking-widest px-3.5 py-1.5"
      >
        What I keep · Sanity Studio
      </span>
    </p>
    <div class="space-y-4 text-gray-600 leading-relaxed text-lg">
      <p>
        <strong class="font-semibold text-gray-900">Curation.</strong> He posts the
        raw work; the Studio is where I make the site look curated. I set the
        featured photos, the order each category shows in, and the cover shots,
        none of it touching his page. When he sends a batch from one job, I group
        them so they read as one project.
      </p>
      <p>
        <strong class="font-semibold text-gray-900">Alt text, automated.</strong>
        Seeding the gallery meant about 100 photos, each one needing alt text for
        accessibility. I wrote a script that ran them through Claude to generate
        the alt text and title from the image itself, then wrote both straight to
        Sanity. I reviewed every one in the Studio, and fixing a label was a
        click, not a hunt through code.
      </p>
      <p>
        <strong class="font-semibold text-gray-900">Maintainability.</strong> His work
        lives in Sanity, not hardcoded in my files, so I'm not the only one who
        can keep the gallery running. Any developer could take it over, and if he
        ever needs more than posting a photo, he can hand it to someone who works
        in Sanity.
      </p>
    </div>
  </Column>

  <Wide class="mt-8">
    <figure>
      <Image
      src={sanityStudioDesk}
      alt="Sanity Studio on desktop showing the custom Content desk I built (Reorder Gallery, Featured Items, Category Covers, Site Images) next to a drill-down from Categories into Concrete, its Walkways & Steps subcategory, and the individual gallery items."
      width={1536}
      class="w-full rounded-xl shadow-xl ring-1 ring-gray-900/10"
      loading="lazy"
    />
    </figure>
  </Wide>

  <!-- Synthesis -->
  <Column class="mt-8">
    <p class="text-sm text-gray-500 leading-relaxed">
      Same database, two doors in. He posts a job; I curate how it shows.
    </p>
  </Column>

  <Prose class="mt-8">
    <p>
      The notes field is doing more than it looks. It's an async channel between
      us: he leaves a note ("put this one first," or "this is the Hendersons'
      backyard"), I see it in Sanity, I handle it. No phone call, no scheduling,
      no "are you free." The tool absorbed the back-and-forth that used to be a
      conversation.
    </p>
  </Prose>

  <Column class="mt-8">
    <p
      class="text-xl font-semibold text-gray-900 leading-snug border-l-4 border-amber-400 pl-5"
    >
      The result is the thing I set out to build: he posts his own work, from his
      phone, in his language, and the site updates itself. He doesn't need me to
      keep his business online.
    </p>
  </Column>
</Section>
