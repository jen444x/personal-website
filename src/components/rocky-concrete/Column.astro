---
// A bare reading-width column (4xl, centered) for non-prose blocks:
// lists, callouts, grids. Same width as Prose so edges line up.
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class:list={["max-w-4xl mx-auto", className]}>
  <slot />
</div>
